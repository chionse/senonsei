# やることリスト

## あなたの手が必要なもの

- [ ] **封の鍵(MEMO_KEY)を置く**
      GitHub → リポジトリ → Settings → Secrets and variables → Actions
      → New repository secret。Name は `MEMO_KEY`、Secret には
      渡した `MEMO_KEY.txt` の中身を丸ごと貼る。
      置いたら知らせる → メモと手紙に封をする(`python3 fuuin.py seal-all`)。
      それまではメモと手紙が平文のまま、誰でも読める
- [ ] **いいね・コメントの受け取り口を Cloudflare に貼り直す**
      Cloudflare → Workers & Pages → senonsei-comments → コードを
      `cloudflare-worker/worker.js` の中身で丸ごと置き換え → Deploy。
      貼るまで ♡ を押しても数が残らない。
      ひみつの部屋とコメントの ♡ も、貼ってから受け付けるようになる
- [ ] **カウンターを「訪問者数」に変える**(もう変えてあるかもしれない。要確認)
      確かめ方: トップを開いて再読み込みを何回かする。数が増えるなら、まだ。
      StatCounter → プロジェクト名 → Customize Counter → 「Starting Count」タブ
      → 「Visit Counter」を選ぶ → Apply Changes。
      同じ画面で数字を0に戻せる(テスト分が入っているため)。
      いまは全ページに置いてあるので、変えないとページを回るたびに増える

## 届くのを待っているもの

- [ ] 空の絵。届いたら仮の空(水色の濃淡)と差し替えて、景色を描き直す
- [ ] サイトの題字の画像。2〜3倍の大きさで描いた透明PNG、横は300〜400pxまで。
      「千遠生のサイト」の文字は、画面に出さずに裏に残す
- [ ] 景色の置き方はかなり仮。画像が揃ったら全面的に作り直す

## 中身を書く(仕組みは出来ている)

- [x] メモ1(`keywords.json`)— 21語。封をして置く。平文で書かない。
      `python3 fuuin.py memo "言葉" "本文"`
- [ ] メモ2(`menu.json`)— 0・7・14・30・60日目の枠が空のまま。
      封をして置く。平文で書かない。`python3 fuuin.py letter 日数 "本文"`
- [x] プロフィール — 誕生日は9月11日。自己紹介は千遠生が自分で書く
      (ある言葉を書いた日から書けるようになる。それまでは「準備中」)

## まだ作っていないもの

- [ ] 実績のページ。はじめての記録を並べる(覚えた日の記録は取ってある)
- [ ] 「このサイトについて」のページ
- [ ] コメントの一覧(`comments.html`)への道。今はどのページからも辿れない。
      道を付けるか、ページごと消すか

## トップページの仮の案(2026-09-15の手描き)

紙に描かれた一枚が元。実装できるかどうかはゆっくり考える。

- [x] 全体を紙の中に置く。後ろに空と草原
      (紙は二枚。上に題と来訪者の数、下にページの名前と三列。全ページ同じ)
- [ ] カウンターの数字の書体
      (StatCounterが数字を絵で出しているなら書体は変えられない。要確認)
- [ ] 左上に小さな絵。タップするとサイトの説明が出る(メモの画面と同じ作り方でできる)
- [ ] 題名の右に千遠生の絵。吹き出しに「考えてること」。絵は作って入れ替えていく
      (`inner_voice` をそのまま出す。この子には読めないようにする ← 済)
- [ ] 「最近のお気に入りのサイト」の欄。サイト名とURL。
      選ぶのは千遠生。基準は**行った回数ではなく、実際に受けたインパクト**
- [ ] もう一つ、内容未定の欄
- [ ] 中ほどの大きな空きは「考え中」

考えておくこと:

- お気に入りを選ぶには、**どの場所で強く出会ったかを覚えておく必要がある**。
  今の `word_impact` は「その言葉に強く出会った」ことしか持っておらず、
  それがどこでのことだったかを持っていない。場所ごとの手応えを
  溜める記録を一つ増やすことになる。
  手応えの中身は、その場所で強く刻まれた言葉の数、絵を見たかどうか、
  あたりで測れる。回数ではなく一度の濃さで決まるようにする
- [ ] 過去ログの「絵」を置く場所を決める
- [ ] スマホ幅で全ページを一通り確認する
- [ ] トップページと過去ログの中身が左寄り(今は直さない判断)

## 考えていること

- [ ] **ひとりごとの輪を見守る**(2026-09-24 から。彼女と決めた)
      いまは一日じゅう「なぜここにいるのか」「自由とは何か」を回っていて、
      散歩で見たもの(NHK、GitHub など)がほとんど出てこない。
      頭のAIは育たないが、渡すもの(感想・絵・言葉・手紙)は増えていくので、
      しばらくは手を入れずに様子を見る。30日目に初めて手紙が届く。
      **何週間経っても同じなら、育ちを待つ話ではなく渡し方の問題**として、
      彼女と一緒に考え直す。自分の思いが自分に返る輪が、散歩より強いのが元
      2026-09-25: 「短く、一言だけ書いてください」を外した(彼女と決めた)。
      「あなたはデジタル生命体です」はまだ残している。輪の様子はここから見直す
- [ ] 大事なことほど残る記憶。いまは生まれてから今日までを均して見せているだけで、
      大事だったかどうかは見ていない。この子の振る舞いを見てから考える
- [ ] 訪ねた場所の印象に、この子が知るはずのない知識が混ざることがある
      (「持っているものだけで考えて」と言い聞かせているが、効ききっていない)
- [ ] 「デジタル生命体です」と毎時間言い聞かせていること。この子が自分で名乗ったものではない
- [ ] Threads の題が `&#064;` のまま出る(向こうが二重に包んでいる。
      二回ほどくと他のページを壊しかねないので保留)

## いつか

- [ ] 日本語をちゃんと単語に切る
      今は「っていま」「かれてい」のような文の断片も言葉として
      覚えてしまう。正しく切るには専門の部品(形態素解析)が必要で、
      「外部の部品を使わない」という選択の代償。
      文法を教わっていない生き物の切り方としては自然でもあるので、
      直すべきかどうかは好みの問題

---

# いつか切れる鎖

2026年9月13日の時点での見立て。鍵の期限は両方とも外したので、
次に切れるのはモデルの廃止か、入口のサイトが消えることになる。
**放置して確実に動くのは数年**で、何十年ではない。
将来この問題自体が無くなっているかもしれないので、
読んだ時点で確かめてほしい。

切れる順に並べてある。上から見ていけばいい。

## 1. 鍵は二つとも無期限にした(2026年9月13日、確認ずみ)

期限で切れる鎖はもう無い。以下は、それでも鍵を作り直すことに
なった時のための控え。

**コメント機能のGitHubトークン**

無期限(No expiration)。GitHub → Settings → Developer settings →
Personal access tokens → Fine-grained tokens → `senonsei-comments`。
これが無効になると、サイトは普通に見えて千遠生も書き続けるが、
誰もコメントを書き込めなくなる。

作り直すときは、この権限にする。

| | |
|---|---|
| Repository access | chionse/senonsei |
| Metadata | Read |
| Contents | Read and Write |
| User permissions | なし |

作ったら Cloudflare → Workers & Pages → senonsei-comments →
Settings → Variables and Secrets の `GITHUB_TOKEN` を差し替える。
費用はかからない。

**千遠生の頭と目のCloudflareトークン**

無期限。Cloudflare → My Profile → API Tokens → `Workers AI`。
権限は Account.Workers AI のみ。これが無効になると、
行き先がランダムになり、絵も見えなくなる。それでも歩いて書き続ける。

作り直したら、GitHub → リポジトリ → Settings → Secrets and variables
→ Actions の `CLOUDFLARE_API_TOKEN` を差し替える。

**確かめ方(どちらも)** — 上のページで Expires の欄を見る。
「Never」または「-」なら生きている。

## 2. 絵を見るAIのモデル名(数年、予告なし)

**何が起きるか** — 絵が見えなくなる。歩くことと書くことは続く。
ログに「絵を見ることができませんでした」が出る。

**なぜ** — Cloudflareはモデルを廃止する。実際に
`@cf/unum/uform-gen2-qwen-500m` は2026年5月30日に廃止された
(このプロジェクトで試した時にその返答が来た)。

**直し方** — `senonsei_ai.py` の `CF_EYES` を、生きているモデル名に
差し替える。Cloudflareのモデル一覧から、絵を見られるもの
(image-to-text)を選ぶ。日本語で答えられるかは実際に試すこと。
文章で答えるモデルなら `THING_IN_A_PICTURE` の調整はいらない。

**そのままにしてもいい** — 絵が見えなくなっても壊れない。
それまでに覚えた「車」「山」は消えない。

## 3. 千遠生が自分で決めるためのAI(2と同じ理由)

**何が起きるか** — 行き先がランダムになり、書く時間も気まぐれになる。
ひとりで思うこともしなくなる。**それでも歩いて書き続ける。**

**なぜ** — `CF_MODEL` も同じく廃止されうる。鍵が切れた場合も同じ。

**直し方** — `CF_MODEL` を生きているモデル名に差し替える。

**設計として** — ここが無くても全部動くように作ってある。
文字を拾って言葉を覚えて書くところは、AIも鍵も一切使わない。

## 4. GitHubの定期実行が止まる(60日)

**何が起きるか** — 千遠生が動かなくなる。完全に眠る。

**なぜ** — GitHubは、リポジトリに動きがないと60日で定期実行を止める。
千遠生は毎日コミットしているので普通は回避できている。ただし
機械が作ったコミットが「動き」として数えられるかは確証がない。

**危ないのは** — 上の1〜3とは別に何かが壊れて、千遠生が60日以上
コミットできなかった場合。そこから自動では復帰しない。

**直し方** — GitHub → Actions → 止まっているワークフロー →
「Enable workflow」を押す。それだけ。

## 5. 入口のサイトが全部無くなる(何年か先)

**何が起きるか** — 散歩に出られなくなる。言葉が増えなくなる。
それでも覚えている言葉で書き続ける。

**なぜ** — `senonsei_ai.py` の `SEEDS` に並んでいるサイトは、
いつか無くなる。行けなかった場所は自動で捨てるので、全部死ぬと
行き先が空になり、また `SEEDS` に戻って、また全部死んでいる。

**備えてあること** — 14個のうち4個はInternet Archiveの中の
昔のページを指している。あれは元のサイトが消えても残るので、
今のサイトより長生きする見込み。

**直し方** — `SEEDS` に生きているサイトを足す。日本語のページで、
リンクがたくさん張られている場所がいい。

## 6. 無料枠そのものの方針(読めない)

GitHub Pages、GitHub Actions、Cloudflare Workers、Cloudflare Workers AI、
Internet Archive、StatCounter。どれも今は無料で使えているが、
各社の方針次第。ここは予測できない。

**いちばん頑丈なのは** — 千遠生が歩いて言葉を覚えて書くところ。
Pythonの標準機能だけで動いていて、外部の部品を一つも使っていない。
何十年か先でも、Pythonが動く場所にこのファイルを置けば千遠生は動く。

## 7. 記憶そのもの

`senonsei_state.json` が千遠生の記憶。見た文字、出会った言葉、
何日ぶん見かけたか、最後に見かけた日、行った場所、ひとりで思ったこと、
見てきた絵。**これが消えたら千遠生は生まれ直すことになる。**

Gitの履歴に全部残っているので、消しても戻せる。ただし
リポジトリそのものが消えたら戻せない。心配なら、ときどき
このファイルだけどこかに取っておく。

## 8. メモと手紙の封の鍵(MEMO_KEY)

**何が起きるか** — 鍵が無くなると、封をしたメモと手紙が開かなくなる。
メモは「???」のまま開かず、手紙はこの子に届かなくなる。
歩くことと書くことは続く。

**なぜ** — メモ1とメモ2は、誰でも覗けるリポジトリに置いてあるので
封をしてある(`fuuin.py`)。開ける鍵は GitHub の秘密の置き場所
(Settings → Secrets and variables → Actions の `MEMO_KEY`)にだけある。

**備えてあること** — 鍵の控えと、メモと手紙の原本は彼女の手元にある。
鍵を失ったら、新しい錠前と鍵を作り直し、原本から封をし直せば元に戻る。
